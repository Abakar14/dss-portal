import { StudentDetailsCreateDto } from './student-details-create-dto.model';

describe('StudentDetailsCreateDto', () => {
  it('should create an instance', () => {
    expect(new StudentDetailsCreateDto()).toBeTruthy();
  });
});
